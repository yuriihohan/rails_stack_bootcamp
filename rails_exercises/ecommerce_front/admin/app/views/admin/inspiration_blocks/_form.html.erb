  <% if @inspiration_block.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@inspiration_block.errors.count, "error") %> prohibited
      this Inspiration Block from being saved:
    </h2>
    <ul>
      <% @inspiration_block.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
  <% end %>

<%= form_for @inspiration_block, :html => { :multipart => true } do |f| -%>
  <div class="field">
    <%= f.label :header %>
    <%= f.text_field :header, class: 'input-xxlarge' %>
  </div>

  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text, cols: 40, rows: 5, class: 'input-xxlarge' %>
  </div>
  <div class="field">
    <%= f.file_field :image %>
  </div>
  <%= f.submit 'Save Inspiration Block', :class => 'btn primary' %>
<% end %>